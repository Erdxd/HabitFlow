package jwt

/*

import "os"

var JwtKey []byte

func InitToken() {
	var JwtKeystr = os.Getenv("JWT_TOKEN")
	if JwtKeystr == "" {
		panic("JWT TOKEN NOT SET IN ENV ")
	}
	JwtKey = []byte(JwtKeystr)

}
*/
