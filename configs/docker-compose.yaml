version: '3.8'

services:
  app:
    build: .
    ports:
      - "8081:8081"
    env_file:
      - Database.env
      - bot.env
    environment:
      - PORT=8081
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
    network_mode: "host"
