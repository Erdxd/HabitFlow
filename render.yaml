
services:
  - type: web
    name: habitflow
    env: go
    buildCommand: go build -o habitflow
    startCommand: ./habitflow
    envVars:
      - key: PORT
        value: 8080
      - key: TELEGRAM_BOT_TOKEN
        sync: false
